name: prays_times_kimi
description: "A new Flutter project for prayer times."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # المكتبات الأساسية
  http: ^1.2.0
  shared_preferences: ^2.2.2
  intl: any
  google_fonts: ^6.1.0
  adhan: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
