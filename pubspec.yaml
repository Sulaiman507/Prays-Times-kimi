name: prays_times_kimi
description: "تطبيق مواقيت الصلاة"
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.10.0 <4.0.0"
  flutter: ">=3.38.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_localizations:
    sdk: flutter

  adhan_dart: ^2.0.1
  flutter_local_notifications: ^22.2.0
  flutter_timezone: ^5.1.0
  geocoding: ^5.0.0
  geolocator: ^14.0.3
  google_fonts: ^8.2.1
  http: ^1.6.0
  intl: ^0.20.2
  shared_preferences: ^2.5.5
  timezone: ^0.11.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
