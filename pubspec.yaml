name: prayer_times_app
description: تطبيق مواقيت صلاة جميل ومتكامل

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # API & Networking
  http: ^1.2.0

  # Local Storage
  shared_preferences: ^2.2.2

  # Date & Time formatting
  intl: ^0.20.2

  # Beautiful Fonts
  google_fonts: ^6.1.0

  # Location (اختياري - لاحقًا)
  # geolocator: ^11.0.0
  # geocoding: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
